//
// Created by maks5507 (Maksim Eremeev, maks5507@yandex.ru)
//


const tabIds = ['#text-tab', '#urls-tab', '#files-tab']

const tabsToDivs = {'#text-tab': '#text-body',
                    '#urls-tab': '#urls-body',
                    '#files-tab': '#files-body'}

const rateId = '#rate'
const sendRateId = '#submit-rating'

const factorRange = '#factor-range'
const factorValue = '#factor-value'

const tabBodyIds = ['#files-body', '#urls-body', '#text-body']

const fileInputId = '#files-upload'
const filesTabId = '#files-badge'

const urlsInputsIds = ['#url-1', '#url-2', '#url-3', '#url-4', '#url-5']

const server = 'http://34.133.236.6:22111/main'

const submitButtonId = '#summarize'

const urlsIds = ['#url-1', '#url-2', '#url-3', '#url-4', '#url-5']

const resultId = '#result ul'

const loadingGifId = '#loading'

const stars = ['#star1', '#star2', '#star3', '#star4', '#star5']
